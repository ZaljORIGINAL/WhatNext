<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zalj.schedule">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver
            android:name="com.zalj.schedule.MyNotifications.MyAlarm"
            android:enabled="true"
            android:exported="true"></receiver>

        <activity android:name="com.zalj.schedule.ScheduleOfDayActivity" />
        <activity
            android:name="com.zalj.schedule.ScheduleBuilderActivity"
            android:label="@string/nameOfActivity_ScheduleCreating" />
        <activity
            android:name="com.zalj.schedule.SelectScheduleActivity"
            android:label="@string/nameOfActivity_SelectSchedule" />
        <activity android:name="com.zalj.schedule.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>